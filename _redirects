# SPA fallback for client-side routing
# 静的ファイル（CSS/JS/画像など）は除外し、拡張子のないパスのみを /index.html にフォールバック
# 既存のファイルが存在する場合は自動的に優先される

# 静的ファイルを明示的に除外（拡張子があるファイルはそのまま提供）
# 拡張子のないパスのみを /index.html にフォールバック
/* /index.html 200

